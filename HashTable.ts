class HashTable<T> {
    get({ i }: { i: number; }): T {
        throw new Error("Method not implemented.");
    }
    put<T extends {}>(obj: T, now: number) {
        
    }
    remove<T>(obj: T) {
    }
    size: number;
    constructor(parameters) {
        
    }
}

export default HashTable;